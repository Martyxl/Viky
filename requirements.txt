# Core deps shared across services (M1).
# Heavy ML deps (faster-whisper, openwakeword, silero-vad, piper-tts, litellm)
# are pulled in per-milestone via requirements-*.txt to keep DEV installs light.

fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
httpx>=0.27,<1.0
python-multipart>=0.0.9

# Dev / test
pytest>=8.0
