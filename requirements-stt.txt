# STT service deps (M3). Install on top of requirements.txt.
faster-whisper>=1.0
silero-vad>=5.1   # pulls torch; VAD itself runs via onnxruntime (load_silero_vad(onnx=True))
soundfile>=0.12
numpy>=1.26
sounddevice>=0.4.7   # scripts/listen.py mic capture
