# STT service deps (M3). Install on top of requirements.txt.
faster-whisper>=1.0
silero-vad>=5.1   # pulls torch; VAD itself runs via onnxruntime (load_silero_vad(onnx=True))
soundfile>=0.12
numpy>=1.26
sounddevice>=0.4.7   # scripts/listen.py mic capture

# GPU (optional): for WHISPER_DEVICE=cuda on Windows/Linux with an NVIDIA GPU.
# faster-whisper (ctranslate2) needs the CUDA 12 cuBLAS/cuDNN runtime; these
# wheels ship the DLLs and common/cuda.py registers them on Windows.
nvidia-cublas-cu12; platform_system != "Darwin"
nvidia-cudnn-cu12; platform_system != "Darwin"
